export const WEB_SHELL_STYLES = "    *, *::before, *::after { box-sizing: border-box; }\n    :root {\n      --font-ui: 'Plus Jakarta Sans', system-ui, -apple-system, BlinkMacSystemFont, sans-serif;\n      --font-mono: 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, 'Apple Color Emoji', 'Segoe UI Emoji', 'Noto Color Emoji', monospace;\n      --bg: oklch(97.5% 0.008 270);\n      --surface: #ffffff;\n      --surface-soft: oklch(97.8% 0.006 270);\n      --surface-muted: oklch(97.5% 0.008 270);\n      --surface-hover: oklch(97% 0.015 270);\n      --field: #ffffff;\n      --border: oklch(91% 0.015 270);\n      --border-soft: oklch(94% 0.01 270);\n      --text: oklch(20% 0.02 270);\n      --text-strong: oklch(18% 0.02 270);\n      --muted: oklch(58% 0.03 270);\n      --accent: oklch(55% 0.2 285);\n      --accent-dark: oklch(40% 0.2 285);\n      --accent-light: oklch(95% 0.055 285);\n      --green: #22c55e;\n      --amber: #f59e0b;\n      --yellow: #eab308;\n      --amber-soft-bg: oklch(97% 0.055 75);\n      --amber-soft-border: oklch(86% 0.11 75);\n      --amber-soft-text: oklch(35% 0.11 75);\n      --red: #f87171;\n      --red-text: oklch(38% 0.18 20);\n      --red-soft-bg: oklch(97% 0.035 20);\n      --red-soft-border: oklch(85% 0.08 20);\n      --scrollbar-thumb: oklch(84% 0.02 270);\n      --terminal: #0d1117;\n      --terminal-bar: #161b22;\n      --terminal-border: #30363d;\n      --terminal-font-size: 12.5px;\n      --font-size-input: 12.5px;\n      --control-h: 32px;\n      --control-h-sm: 26px;\n      --control-px: 10px;\n      --control-fs: 12.5px;\n      --terminal-line-height: 1.16;\n      --terminal-fallback-line-height: 1.25;\n      --terminal-xterm-padding: 6px 8px;\n      --terminal-fallback-padding: 10px 12px;\n      --sidebar-width: 220px;\n      color-scheme: light;\n    }\n    :root[data-theme=\"dark\"] {\n      --bg: color-mix(in srgb, #0b1020 90%, var(--accent) 10%);\n      --surface: color-mix(in srgb, #111827 92%, var(--accent) 8%);\n      --surface-soft: color-mix(in srgb, #0f172a 92%, var(--accent) 8%);\n      --surface-muted: color-mix(in srgb, #111827 92%, var(--accent) 8%);\n      --surface-hover: color-mix(in srgb, #1f2937 90%, var(--accent) 10%);\n      --field: color-mix(in srgb, #0f172a 94%, var(--accent) 6%);\n      --border: color-mix(in srgb, #26324a 88%, var(--accent) 12%);\n      --border-soft: #1f2937;\n      --text: #e5e7eb;\n      --text-strong: #f8fafc;\n      --muted: #9ca3af;\n      --accent: #a78bfa;\n      --accent-dark: #ddd6fe;\n      --accent-light: #2e2147;\n      --yellow: #facc15;\n      --amber-soft-bg: oklch(28% 0.05 75);\n      --amber-soft-border: oklch(45% 0.12 75);\n      --amber-soft-text: oklch(88% 0.08 75);\n      --red-text: #fca5a5;\n      --red-soft-bg: #3b1518;\n      --red-soft-border: #7f1d1d;\n      --scrollbar-thumb: #2c3a55;\n      color-scheme: dark;\n    }\n    @media (prefers-color-scheme: dark) {\n      :root[data-theme=\"auto\"] {\n        --bg: color-mix(in srgb, #0b1020 90%, var(--accent) 10%);\n        --surface: color-mix(in srgb, #111827 92%, var(--accent) 8%);\n        --surface-soft: color-mix(in srgb, #0f172a 92%, var(--accent) 8%);\n        --surface-muted: color-mix(in srgb, #111827 92%, var(--accent) 8%);\n        --surface-hover: color-mix(in srgb, #1f2937 90%, var(--accent) 10%);\n        --field: color-mix(in srgb, #0f172a 94%, var(--accent) 6%);\n        --border: color-mix(in srgb, #26324a 88%, var(--accent) 12%);\n        --border-soft: #1f2937;\n        --text: #e5e7eb;\n        --text-strong: #f8fafc;\n        --muted: #9ca3af;\n        --accent: #a78bfa;\n        --accent-dark: #ddd6fe;\n        --accent-light: #2e2147;\n        --yellow: #facc15;\n        --amber-soft-bg: oklch(28% 0.05 75);\n        --amber-soft-border: oklch(45% 0.12 75);\n        --amber-soft-text: oklch(88% 0.08 75);\n        --red-text: #fca5a5;\n        --red-soft-bg: #3b1518;\n        --red-soft-border: #7f1d1d;\n        --scrollbar-thumb: #2c3a55;\n        color-scheme: dark;\n      }\n    }\n    body { margin: 0; height: 100vh; overflow: hidden; background: var(--bg); color: var(--text); font-family: var(--font-ui); -webkit-font-smoothing: antialiased; }\n    button, input, select { font: inherit; }\n    button { border: 0; cursor: pointer; }\n    ::-webkit-scrollbar { width: 6px; height: 6px; }\n    ::-webkit-scrollbar-track { background: transparent; }\n    ::-webkit-scrollbar-thumb { background: var(--scrollbar-thumb); border-radius: 3px; }\n    @keyframes livePulse { 0% { transform: scale(1); opacity: .4; } 60%, 100% { transform: scale(2.4); opacity: 0; } }\n\n    .app { display: flex; height: 100vh; min-height: 0; overflow: hidden; }\n    .sidebar { width: 220px; height: 100vh; min-height: 0; background: var(--surface); border-right: 1px solid var(--border); display: flex; flex-direction: column; flex-shrink: 0; transition: width .16s ease; }\n    .brand { min-height: 56px; padding: 16px 14px 14px; border-bottom: 1px solid var(--border-soft); display: flex; align-items: center; gap: 10px; }\n    .brand-logo { width: 28px; height: 28px; border-radius: 7px; background: var(--accent); display: flex; align-items: center; justify-content: center; color: white; flex-shrink: 0; }\n    .brand-text { min-width: 0; flex: 1; }\n    .brand-title { font-size: 13px; font-weight: 800; letter-spacing: -0.01em; font-family: 'Epilogue', 'Plus Jakarta Sans', sans-serif; background-image: linear-gradient(135deg, var(--text-strong) 0%, var(--accent) 60%, var(--accent-dark) 100%); background-clip: text; -webkit-background-clip: text; color: transparent; -webkit-text-fill-color: transparent; }\n    [data-theme=\"dark\"] .brand-title { background-image: linear-gradient(135deg, #f0f6ff 30%, var(--accent) 100%); }\n    @media (prefers-color-scheme: dark) { :root[data-theme=\"auto\"] .brand-title { background-image: linear-gradient(135deg, #f0f6ff 30%, var(--accent) 100%); } }\n    .sidebar-toggle { width: 24px; height: 24px; border-radius: 7px; background: var(--surface-soft); border: 1px solid var(--border-soft); color: var(--muted); display: inline-flex; align-items: center; justify-content: center; flex-shrink: 0; }\n    .sidebar-toggle:hover { background: var(--surface-hover); color: var(--text); }\n    .sidebar-toggle-icon { transition: transform .16s ease; }\n    :root[data-sidebar=\"collapsed\"] .sidebar { width: 64px; }\n    :root[data-sidebar=\"collapsed\"] .brand { padding: 12px 8px; justify-content: center; gap: 4px; }\n    :root[data-sidebar=\"collapsed\"] .brand-text, :root[data-sidebar=\"collapsed\"] .nav-label, :root[data-sidebar=\"collapsed\"] .nav-badge, :root[data-sidebar=\"collapsed\"] .footer-main { display: none; }\n    :root[data-sidebar=\"collapsed\"] .nav button { justify-content: center; padding: 9px 0; }\n    :root[data-sidebar=\"collapsed\"] .sidebar-footer { justify-content: center; padding: 12px 8px; }\n    :root[data-sidebar=\"collapsed\"] .sidebar-toggle-icon { transform: rotate(180deg); }\n    .nav { flex: 1; padding: 10px 8px; }\n    .nav button { width: 100%; display: flex; align-items: center; gap: 10px; padding: 9px 12px; border-radius: 8px; background: transparent; color: var(--muted); font-size: 13px; font-weight: 500; margin-bottom: 2px; transition: background .1s, color .1s; }\n    .nav button:hover { background: var(--surface-hover); }\n    .nav button.active { background: var(--accent-light); color: var(--accent-dark); font-weight: 700; }\n    .nav svg { flex-shrink: 0; }\n    .nav-badge { margin-left: auto; min-width: 18px; height: 18px; border-radius: 9px; padding: 0 5px; background: var(--accent); color: #fff; display: inline-flex; align-items: center; justify-content: center; font-size: 10px; font-weight: 700; }\n    .sidebar-footer { padding: 12px 14px; border-top: 1px solid var(--border-soft); display: flex; align-items: center; gap: 8px; }\n    .footer-title { font-size: 11px; font-weight: 600; }\n    .footer-sub { margin-top: 1px; font-family: var(--font-mono); font-size: 10px; color: oklch(60% 0.02 270); }\n\n    .shell { min-width: 0; flex: 1; height: 100vh; min-height: 0; display: flex; flex-direction: column; }\n    .topbar { height: 56px; background: var(--surface); border-bottom: 1px solid var(--border); display: flex; align-items: center; gap: 16px; padding: 0 28px; flex-shrink: 0; }\n    .top-title { flex: 1; min-width: 0; }\n    .top-title h1 { margin: 0; font-size: 16px; font-weight: 700; color: var(--text-strong); letter-spacing: -0.01em; }\n    .top-title div { margin-top: 1px; font-size: 11px; color: var(--muted); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }\n    .top-actions { display: flex; align-items: center; gap: 10px; }\n    .content { flex: 1; min-height: 0; padding: 28px; overflow: hidden; }\n\n    .btn { display: inline-flex; align-items: center; justify-content: center; gap: 7px; padding: 8px 18px; border-radius: 8px; background: var(--accent); color: #fff; font-size: 13px; font-weight: 700; box-shadow: 0 2px 8px color-mix(in srgb, var(--accent) 30%, transparent); }\n    .btn:hover { background: color-mix(in srgb, var(--accent) 82%, var(--text) 18%); }\n    .btn.secondary { background: var(--surface); color: var(--text); border: 1px solid var(--border); box-shadow: none; }\n    .btn.secondary:hover { background: var(--surface-hover); }\n    .btn.small { min-height: var(--control-h); padding: 0 var(--control-px); border-radius: 7px; font-size: var(--control-fs); }\n    .btn.danger { background: var(--red); color: #fff; border: 1px solid color-mix(in srgb, var(--red) 70%, black 30%); box-shadow: 0 2px 8px color-mix(in srgb, var(--red) 28%, transparent); }\n    .btn.danger:not(:disabled):hover, .btn.danger:not(:disabled):focus-visible { background: color-mix(in srgb, var(--red) 78%, black 22%); border-color: color-mix(in srgb, var(--red) 60%, black 40%); color: #fff; box-shadow: 0 0 0 3px color-mix(in srgb, var(--red) 22%, transparent); }\n    .btn.secondary.danger { background: var(--surface); color: var(--red-text); border: 1px solid color-mix(in srgb, var(--red) 42%, var(--border)); box-shadow: none; }\n    .btn.secondary.danger:not(:disabled):hover, .btn.secondary.danger:not(:disabled):focus-visible { background: color-mix(in srgb, var(--red) 12%, var(--surface)); border-color: var(--red); color: var(--red-text); box-shadow: 0 0 0 3px color-mix(in srgb, var(--red) 14%, transparent); }\n    .btn:disabled { opacity: .55; cursor: not-allowed; box-shadow: none; }\n\n    .live-dot { position: relative; display: inline-block; width: 8px; height: 8px; flex-shrink: 0; }\n    .live-dot::before { content: ''; position: absolute; inset: 0; border-radius: 50%; background: currentColor; opacity: .4; animation: livePulse 2s ease-out infinite; }\n    .live-dot::after { content: ''; position: absolute; inset: 0; border-radius: 50%; background: currentColor; }\n    .status-pill { display: inline-flex; align-items: center; gap: 6px; padding: 4px 10px; border-radius: 7px; background: oklch(95% 0.04 150); border: 1px solid oklch(88% 0.07 150); color: oklch(38% 0.14 150); font-family: var(--font-mono); font-size: 11px; font-weight: 600; }\n    .badge { display: inline-flex; align-items: center; gap: 5px; padding: 2px 8px; border-radius: 6px; border: 1px solid oklch(88% 0.03 270); background: oklch(96% 0.01 270); color: oklch(50% 0.04 270); font-size: 11px; font-weight: 600; letter-spacing: .02em; white-space: nowrap; }\n    .badge.claude { background: oklch(95% 0.05 285); color: oklch(40% 0.18 285); border-color: oklch(85% 0.08 285); }\n    .badge.openc { background: oklch(96% 0.04 160); color: oklch(38% 0.14 160); border-color: oklch(86% 0.07 160); }\n    .badge.live { background: oklch(95% 0.04 150); color: oklch(38% 0.14 150); border-color: oklch(86% 0.07 150); }\n    .badge.pending { background: oklch(96% 0.04 75); color: oklch(45% 0.16 75); border-color: oklch(88% 0.08 75); }\n\n    .card { background: var(--surface); border: 1px solid var(--border); border-radius: 12px; box-shadow: 0 1px 4px oklch(0% 0 0 / .04); }\n    .card-pad { padding: 20px; }\n    .section-head { display: flex; align-items: center; justify-content: space-between; gap: 14px; padding-bottom: 14px; margin-bottom: 16px; border-bottom: 1px solid var(--border-soft); }\n    .section-head h2 { margin: 0; font-size: 14px; font-weight: 700; color: var(--text-strong); }\n    .section-head p { margin: 2px 0 0; font-size: 12px; color: var(--muted); }\n    .grid-stats { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 16px; margin-bottom: 24px; }\n    .stat { padding: 20px; }\n    .stat-label { display: flex; align-items: center; gap: 6px; margin-bottom: 10px; color: var(--muted); font-size: 11px; font-weight: 600; letter-spacing: .06em; text-transform: uppercase; }\n    .stat-value { color: var(--text-strong); font-family: var(--font-mono); font-size: 28px; font-weight: 700; line-height: 1; letter-spacing: -.02em; }\n    .stat-sub { margin-top: 6px; color: var(--muted); font-size: 12px; }\n    .two-col { display: grid; grid-template-columns: 1fr 2fr; gap: 16px; margin-bottom: 24px; }\n    .overview-page { height: 100%; min-height: 0; overflow: auto; }\n    .kv-row { display: flex; align-items: center; gap: 12px; padding: 8px 0; border-bottom: 1px solid oklch(95% 0.008 270); }\n    .kv-row:last-child { border-bottom: 0; }\n    .kv-label { width: 150px; flex-shrink: 0; color: var(--muted); font-size: 12px; font-weight: 500; }\n    .kv-value { color: var(--text); font-size: 13px; word-break: break-word; }\n    .mono { font-family: var(--font-mono); }\n\n    .type-row { display: flex; align-items: center; gap: 10px; margin-bottom: 10px; }\n    .type-row:last-child { margin-bottom: 0; }\n    .type-label { flex: 1; font-size: 13px; font-weight: 500; }\n    .type-count { font-family: var(--font-mono); font-size: 20px; font-weight: 700; }\n    .bar { width: 80px; height: 6px; border-radius: 4px; overflow: hidden; background: oklch(94% 0.01 270); }\n    .bar span { display: block; height: 100%; border-radius: 4px; }\n    .queue-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 16px; }\n    .queue-item { text-align: center; padding: 16px 12px; border-radius: 10px; background: var(--surface-soft); border: 1px solid var(--border-soft); }\n    .queue-value { margin-top: 8px; font-family: var(--font-mono); font-size: 32px; font-weight: 800; line-height: 1; }\n    .queue-desc { margin-top: 6px; color: oklch(62% 0.02 270); font-size: 11px; }\n\n    .peer-icon { display: inline-flex; align-items: center; justify-content: center; width: 28px; height: 28px; border-radius: 9px; font-family: var(--font-mono); font-size: 13px; font-weight: 800; flex-shrink: 0; }\n    .peer-icon.claude { background: oklch(92% 0.07 285); color: oklch(42% 0.2 285); }\n    .peer-icon.openc { background: oklch(93% 0.05 155); color: oklch(38% 0.15 155); }\n    .agent-page { display: flex; flex-direction: column; width: 100%; min-width: 0; height: 100%; min-height: 0; }\n    .tabbar { display: flex; align-items: stretch; min-height: 46px; background: var(--surface); border: 1px solid var(--border); border-radius: 12px 12px 0 0; overflow: hidden; flex-shrink: 0; }\n    .tabbar-scroll { display: flex; flex: 1; overflow-x: auto; }\n    .term-tab { display: flex; align-items: center; gap: 8px; height: 46px; padding: 0 16px; border-right: 1px solid var(--border-soft); border-bottom: 2px solid transparent; background: transparent; color: oklch(50% 0.03 270); font-size: 12.5px; font-weight: 600; white-space: nowrap; }\n    .term-tab.active { border-bottom-color: var(--accent); background: var(--accent-light); color: var(--accent-dark); }\n    .term-tab.terminal-active { background: var(--terminal); color: #d4d4d4; font-family: var(--font-mono); }\n    .tab-new { color: var(--accent); font-weight: 700; }\n    .tab-info { padding: 0 14px; color: var(--muted); font-size: 11px; pointer-events: none; }\n    @keyframes attentionBlink { 0%, 100% { opacity: 1; transform: scale(1); } 50% { opacity: .35; transform: scale(.72); } }\n    .agent-tab-dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; box-shadow: 0 0 0 1px oklch(0% 0 0 / .15); }\n    .agent-tab-dot.online { background: var(--green); }\n    .agent-tab-dot.attention { background: var(--green); animation: attentionBlink 1s steps(2, end) infinite; }\n    .agent-tab-dot.offline { background: var(--red); }\n    .tab-content { flex: 1; width: 100%; min-width: 0; min-height: 0; display: flex; flex-direction: column; background: var(--surface); border: 1px solid var(--border); border-top: 0; border-radius: 0 0 12px 12px; overflow: hidden; }\n    .agent-overview { flex: 1; overflow: auto; padding: 20px; }\n    .launch-strip { display: flex; align-items: center; justify-content: space-between; gap: 16px; margin-bottom: 16px; }\n    .type-tiles { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }\n    .type-tile { display: flex; align-items: center; gap: 12px; padding: 10px 14px; background: var(--surface-muted); border: 1px solid var(--border); border-radius: 9px; }\n    .agent-group-title { display: flex; align-items: center; gap: 10px; margin: 22px 0 12px; font-size: 13px; font-weight: 700; color: var(--text); }\n    .agent-list { display: flex; flex-direction: column; gap: 10px; }\n    .role-card { border: 1px solid var(--border); border-radius: 10px; background: var(--surface); overflow: hidden; }\n    .role-card-main { border-color: color-mix(in srgb, var(--accent) 38%, var(--border)); background: color-mix(in srgb, var(--accent-light) 28%, var(--surface)); }\n    .role-row { display: flex; align-items: center; gap: 12px; padding: 12px 16px; }\n    .role-main { flex: 1; min-width: 0; }\n    .role-name { display: flex; align-items: center; gap: 8px; font-size: 13px; font-weight: 700; }\n    .role-path { margin-top: 2px; color: var(--muted); font-size: 11px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }\n    .role-summary { color: var(--muted); font-size: 11px; margin-right: 6px; white-space: nowrap; }\n    .role-actions { display: flex; align-items: center; gap: 8px; flex-shrink: 0; }\n    .launch-split { position: relative; display: inline-flex; align-items: stretch; }\n    .launch-split .btn { border-radius: 8px 0 0 8px; }\n    .launch-arrow { width: 31px; border-left: 1px solid oklch(100% 0 0 / .35); border-radius: 0 8px 8px 0; background: var(--accent); color: #fff; font-size: 11px; font-weight: 800; }\n    .launch-arrow:hover { background: color-mix(in srgb, var(--accent) 82%, var(--text) 18%); }\n    .launch-menu { position: absolute; z-index: 30; top: calc(100% + 6px); right: 0; min-width: 150px; padding: 6px; border: 1px solid var(--border); border-radius: 9px; background: var(--surface); box-shadow: 0 12px 36px oklch(0% 0 0 / .14); }\n    .launch-menu button { width: 100%; display: flex; align-items: center; gap: 8px; padding: 8px 9px; border-radius: 7px; background: transparent; color: var(--text); text-align: left; font-size: 12px; font-weight: 700; }\n    .launch-menu button:hover { background: var(--surface-hover); }\n\n    .terminal { flex: 1; width: 100%; min-width: 0; min-height: 0; display: flex; flex-direction: column; background: var(--terminal); }\n    .terminal-body { flex: 1; width: 100%; min-width: 0; overflow: auto; padding: var(--terminal-fallback-padding); color: #d4d4d4; font-family: var(--font-mono); font-size: var(--terminal-font-size); line-height: var(--terminal-fallback-line-height); white-space: pre-wrap; }\n    .terminal.xterm-enabled .terminal-body { padding: 0; white-space: normal; }\n    .terminal.xterm-enabled .terminal-input { display: none; }\n    .terminal-body .xterm { width: 100%; height: 100%; padding: var(--terminal-xterm-padding); }\n    .terminal-body .xterm-viewport { background: var(--terminal) !important; }\n    .terminal-input { display: flex; align-items: center; gap: 10px; padding: 10px 14px; border-top: 1px solid var(--terminal-border); background: var(--terminal-bar); flex-shrink: 0; }\n    .terminal-input span { color: #6b7280; font-family: var(--font-mono); font-size: 12px; }\n    .terminal-input input { flex: 1; border: 0; outline: 0; background: transparent; color: #d4d4d4; font-family: var(--font-mono); font-size: 12px; }\n    .terminal-input kbd { padding: 2px 6px; border: 1px solid #30363d; border-radius: 4px; color: #6b7280; font-family: var(--font-mono); font-size: 10px; }\n\n    .messages-page { display: flex; flex-direction: column; height: 100%; min-height: 0; gap: 0; }\n    .message-tabbar + .inbox-panel { border-top: 0; border-radius: 0 0 12px 12px; }\n    .inbox-panel { flex: 1; min-height: 0; display: flex; overflow: hidden; }\n    .conversation-list { width: 280px; flex-shrink: 0; border-right: 1px solid var(--border); overflow-y: auto; overflow-x: hidden; }\n    .conversation { width: 100%; min-width: 0; display: flex; align-items: center; gap: 11px; padding: 12px 16px; border: 0; border-bottom: 1px solid var(--border-soft); background: transparent; text-align: left; }\n    .conversation.active { background: var(--accent-light); }\n    .conversation-main { min-width: 0; flex: 1; }\n    .conversation-title { display: flex; align-items: center; gap: 8px; min-width: 0; }\n    .conversation-name { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-size: 13px; font-weight: 700; }\n    .conversation-preview { max-width: 100%; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; margin-top: 3px; color: var(--muted); font-family: var(--font-mono); font-size: 12px; }\n    .conversation .badge { flex-shrink: 0; }\n    .thread-panel { flex: 1; min-width: 0; display: flex; flex-direction: column; background: var(--surface-soft); }\n    .thread-head { display: flex; align-items: center; gap: 12px; padding: 14px 20px; background: var(--surface); border-bottom: 1px solid var(--border); }\n    .thread-body { flex: 1; overflow: auto; padding: 20px 24px; }\n    .bubble { max-width: 68%; margin-bottom: 14px; padding: 10px 14px; border-radius: 14px 14px 14px 4px; background: var(--surface); border: 1px solid var(--border); box-shadow: 0 1px 3px oklch(0% 0 0 / .06); font-family: var(--font-ui); font-size: 13px; line-height: 1.5; }\n    .bubble.mine { margin-left: auto; border-radius: 14px 14px 4px 14px; background: var(--accent); color: #fff; border-color: transparent; }\n    .bubble.rejected { border-color: var(--red-soft-border); background: var(--red-soft-bg); color: var(--red-text); }\n    .bubble.rejected .bubble-meta { color: var(--red-text); }\n    .bubble.mine.rejected { color: var(--red-text); }\n    .bubble.mine.rejected .bubble-meta { color: var(--red-text); }\n    .bubble-meta { margin-bottom: 6px; color: var(--muted); font-family: var(--font-ui); font-size: 10px; font-weight: 700; letter-spacing: .04em; text-transform: uppercase; }\n    .bubble.mine .bubble-meta { color: color-mix(in srgb, #fff 85%, var(--accent) 15%); }\n    .thread-empty { display: flex; min-height: 220px; align-items: center; justify-content: center; color: var(--muted); font-size: 13px; text-align: center; }\n    .compose { display: flex; gap: 10px; padding: 14px 16px; background: var(--surface); border-top: 1px solid var(--border); }\n    .compose textarea { flex: 1; min-height: 58px; max-height: 140px; resize: vertical; border: 1px solid var(--border); border-radius: 10px; padding: 10px 12px; outline: 0; background: var(--field); color: var(--text); font-family: var(--font-mono); font-size: 12.5px; line-height: 1.4; }\n    .compose textarea:focus { border-color: var(--accent); box-shadow: 0 0 0 3px color-mix(in srgb, var(--accent) 18%, transparent); }\n    .compose button:disabled, .compose textarea:disabled { opacity: .5; cursor: not-allowed; }\n    .send-error { padding: 10px 14px; margin: 0 16px 12px; color: var(--red-text); font-size: 12px; background: var(--red-soft-bg); border: 1px solid var(--red-soft-border); border-radius: 8px; }\n\n    .settings-page { display: flex; flex-direction: column; height: 100%; min-height: 0; gap: 0; }\n    .settings-panel { flex: 1; min-height: 0; overflow: auto; background: var(--surface); border: 1px solid var(--border); border-top: 0; border-radius: 0 0 12px 12px; padding: 20px; }\n    .settings-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 16px; }\n    .settings-wide { grid-column: 1 / -1; }\n    .setting-row { padding: 12px 0; border-bottom: 1px solid var(--border-soft); }\n    .setting-row:last-child { border-bottom: 0; padding-bottom: 0; }\n    .setting-title { font-size: 13px; font-weight: 700; color: var(--text); }\n    .setting-sub { margin: 3px 0 10px; color: var(--muted); font-size: 12px; }\n    .segmented { display: flex; flex-wrap: wrap; gap: 8px; }\n    .seg-option { padding: 7px 11px; border: 1px solid var(--border); border-radius: 8px; background: var(--field); color: var(--text); font-size: 12px; font-weight: 700; }\n    .seg-option.active { border-color: var(--accent); background: var(--accent-light); color: var(--accent-dark); }\n    .setting-select { min-width: 130px; min-height: var(--control-h); padding: 0 var(--control-px); border: 1px solid var(--border); border-radius: 8px; background: var(--field); color: var(--text); font-size: var(--control-fs); }\n    .runner-list { display: flex; flex-direction: column; gap: 8px; }\n    .runner-diag { display: grid; grid-template-columns: 1fr auto; gap: 4px 12px; padding: 10px 12px; border: 1px solid var(--border-soft); border-radius: 9px; background: var(--surface-soft); }\n    .runner-diag-main { display: flex; align-items: center; gap: 8px; min-width: 0; }\n    .runner-diag-meta { grid-column: 1 / -1; color: var(--muted); font-family: var(--font-mono); font-size: 11px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }\n    .shortcut-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px; }\n    .shortcut { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 9px 11px; border: 1px solid var(--border-soft); border-radius: 8px; background: var(--surface-soft); color: var(--muted); font-size: 12px; }\n    .shortcut kbd { border: 1px solid var(--border); border-radius: 5px; padding: 2px 6px; background: var(--field); color: var(--text); font-family: var(--font-mono); font-size: 11px; white-space: nowrap; }\n    .modal-backdrop { position: fixed; inset: 0; z-index: 1000; display: flex; align-items: center; justify-content: center; padding: 20px; background: oklch(0% 0 0 / .4); backdrop-filter: blur(4px); }\n    .modal-backdrop.hidden { display: none; }\n    .modal { width: min(520px, 100%); max-height: calc(100vh - 40px); overflow: auto; background: var(--surface); color: var(--text); border: 1px solid var(--border); border-radius: 16px; padding: 28px; box-shadow: 0 20px 60px oklch(0% 0 0 / .18); }\n    .modal-title { font-size: 16px; font-weight: 800; margin-bottom: 6px; }\n    .modal-sub { color: var(--muted); font-size: 12px; margin-bottom: 24px; }\n    .field-label { display: block; margin: 0 0 8px; color: var(--muted); font-size: 11px; font-weight: 700; letter-spacing: .06em; text-transform: uppercase; }\n    .choice-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 20px; }\n    .choice { padding: 12px 14px; border-radius: 10px; border: 1.5px solid var(--border); background: var(--surface); color: var(--text); text-align: left; }\n    .choice.active { border: 2px solid var(--accent); background: var(--accent-light); }\n    .role-picker { display: flex; flex-direction: column; gap: 6px; max-height: 190px; overflow: auto; margin-bottom: 20px; }\n    .role-pick { display: flex; align-items: center; gap: 10px; padding: 9px 12px; border-radius: 8px; border: 1px solid var(--border); background: var(--surface-soft); color: var(--text); text-align: left; }\n    .role-pick.active { border: 1.5px solid var(--accent); background: var(--accent-light); }\n    .modal input { width: 100%; min-height: var(--control-h); padding: 0 var(--control-px); border-radius: 8px; border: 1px solid var(--border); background: var(--field); color: var(--text); font-family: var(--font-mono); font-size: var(--control-fs); outline: 0; }\n    .modal-actions { display: flex; justify-content: flex-end; gap: 10px; margin-top: 24px; }\n\n    .hidden { display: none !important; }\n    @media (max-width: 1060px) { .grid-stats, .queue-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); } .two-col, .settings-grid { grid-template-columns: 1fr; } }\n    @media (max-width: 760px) { :root[data-sidebar=\"expanded\"] .sidebar { width: 180px; } .topbar { padding: 0 16px; } .top-actions { gap: 6px; } .content { padding: 16px; } .grid-stats, .queue-grid, .type-tiles { grid-template-columns: 1fr; } .inbox-panel { flex-direction: column; } .conversation-list { width: 100%; max-height: 220px; border-right: 0; border-bottom: 1px solid var(--border); } }";
